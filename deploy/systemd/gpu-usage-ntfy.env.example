# Used by scripts/install-systemd.sh and scripts/fix-common-issues.sh when
# token_env = "NTFY_TOKEN" is selected.
# Installed to /etc/gpu-usage-ntfy.env with mode 600.

NTFY_TOKEN=replace_with_rotated_token
